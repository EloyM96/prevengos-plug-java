hub:
  sqlserver:
    url: jdbc:h2:mem:hubbackend;MODE=MSSQLServer;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jobs:
    rrhh-export:
      base-dir: ./build/test-exports
spring:
  jackson:
    property-naming-strategy: SNAKE_CASE
